export const getRoutePath = (filename: string, TRIP_ID: string) => `trips/${TRIP_ID}/route/${filename}`
export const getMemoriesPath = (filename: string, TRIP_ID: string) => `trips/${TRIP_ID}/memories/${filename}`
